#!/bin/bash

# Enable git completion
source ~/.git-completion.sh

# A shorter way to run git status
function gits {
    git status
}

# An updater function so that pulls are fast-forward merged.
function gitup {
    git remote update -p
    git merge --ff-only @{u}
}